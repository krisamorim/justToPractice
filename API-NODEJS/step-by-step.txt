1- inicar o projeto (criar o json)
-> npm init

2- Instalar express (framework mais conhecido no meio node), body-parser (responsavel por realizar a serialização das informações que serão disponibilizadas), CORS (Segurança, para informar a natureza da requisição que eu espero, ou seja, liberar somente para algumas origem), nodemon (ajuda no deve para dar o refresh), morgan (faz registro das requisições).
-> npm i express body-parser cors nodemon morgan

3- criar o arquivo index.js na raiz do projeto (mesmo local onde está o package.json)

4- add alinha abixo no script no json
Removendo essa:
 "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  
E add essa:
 "scripts": {
    "start": "nodemon index.js"
  },